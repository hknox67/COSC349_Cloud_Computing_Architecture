Cosc349 Assignment

Running the system

 To run the vagrant files and launch the system. In the terminal, navigate to the directory containing the vagrantfile and input the command "vagrant up".
 Once the VMs are finished launching you can visit 192.168.56.11:80 to view the client-side interface or 192.168.5613:8700 to view the administration portal.
